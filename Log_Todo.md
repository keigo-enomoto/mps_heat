
## To Do

- 初期配置を生成
- 位置と速度の更新を確認する
- 無次元化について


## Log
24/9/3
- Homebrew で `eigen` とか `gcc` を導入
- `home/local/bin` に gccの `gcc` とか `g++` が入っている

24/9/15
- `test/eigen` で `iccg_eigen.cpp` が正常に動作していることを確認
- 各クラスのファイルをひとまず作成した
- `InputParams.hpp` の仕様を決定
- `Parameters.cpp` の使用を決定
- `main.cpp` を作成。インプットパラメータの読み込みに成功！！

